import { FC, memo } from "react";
import { ComponentDecorator } from "../../../common/ComponentDecorator";

export const BookReference: FC = memo(() => {
    return (
        <ComponentDecorator name="BookReference">
            
        </ComponentDecorator>
    );
});